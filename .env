Lo que falta de Phase 6:
1. ❌ Optional chaining (?.) — parsea, no genera código
2. ❌ Error types (!) — parsea, no genera código
3. ❌ Contracts (contract/implements) — parsea, no lowering
4. 🔶 For Range (for i in 0..10) — parsea, no codegen
5. ❌ const fn — solo en docs
6. ❌ RAII release para dict — kl_dict_free no está en ownership pass (es在中 riesgo bajo: se filtra memoria pero no crashea)
7. ❌ LSP completion / hover / goto-def