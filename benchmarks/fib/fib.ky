# Kyle — Fibonacci (i32, 500M iterations)
fn fib(n: i32) i32:
    a: ^i32 = 0
    b: ^i32 = 1
    i: ^i32 = 0
    while i < n:
        tmp := a + b
        a = b
        b = tmp
        i = i + 1
    b

fn main() i32:
    result := fib(500000000)
    println(result.to_str())
    0
