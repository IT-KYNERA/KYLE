# Kyle — String concatenation (500k iterations)
fn main() i32:
    sb := str_builder.new(500000)
    i = 0
    while i < 500000:
        str_builder.append(^&sb, "x")
        i = i + 1
    result := str_builder.to_str(^&sb)
    str_builder.free(^&sb)
    println(result.len().to_str())
    0
