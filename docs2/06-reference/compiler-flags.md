# Compiler Flags

| Flag | Description |
|------|-------------|
| `--release` | Optimized build with SSA and LLVM O3 |
| `--emit-ir` | Dump LLVM IR to file |
| `--target` | Target triple (default: host) |
