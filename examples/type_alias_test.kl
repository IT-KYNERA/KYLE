type Age = i32
type Years = Age

fn add_one(a: Age) -> Age:
    a + 1

fn double(y: Years) -> Years:
    y * 2

fn main():
    result = add_one(41)
    println(result)
    d = double(50)
    println(d)
