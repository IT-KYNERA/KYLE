# Test: polymorphism via method override.
fn main(args: [str]) -> i32:
    a = Animal()
    d = Dog()
    a.speak()
    d.speak()
    return 0

class Animal:
    fn speak():
        println("generic animal sound")

class Dog: Animal
    fn speak():
        println("Woof!")