import math

fn main() -> i32:
    println(str(absolute(-5)))
    println(str(pow(2, 10)))
    x = gcd(12, 8)
    println(str(x))
    y = gcd(48, 18)
    println(str(y))
    z = clamp(100, 0, 50)
    println(str(z))
    return 0
